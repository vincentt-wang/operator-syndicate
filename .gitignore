
_*.md
_pending-newsroom/
.DS_Store
*.bak
*.bak_*
*.csv
*.doc
*.docx
*.key
*.numbers
*.pages
*.pdf
*.ppt
*.pptx
*.xls
*.xlsx
*.zip
**/_*.md
**/.DS_Store
#
# ═══════════════════════════════════════════════════════════════
# ── macOS 系統檔 ──
# ── 內部草稿一律底線前綴，永不上線 ──
# ── 團隊名片印刷檔（Team/ 的公開頭像會上線，名片不上線）──
# ── 文件／簡報／試算表類：原始資料預設一律不上線 ──
# ── 本機備份檔，不上線 ──
# 07_素材、_archive）都在這個資料夾的「外面」，物理上進不了 repo。
# 2026-07-25 起：這個資料夾「就是」repo 根目錄，整個 repo 只裝會上線的東西。
# EvoScale 官網 repo（public，evoscalecapital.com）
# 所有內部資料（02_評案、03_引擎、04_生態與合作、05_內部文件、06_營運紀錄、
# 新增內部檔案 → 放到外層對應編號夾，不要放進 01_網站/。
insights/_*.html
newsroom/
Team/名片/
